const IP = "localhost";
const PORT = 50541;
const message = {
  connection: "Connecting ...",
  connectionSuccessful: "Successfully connected to game server!"
};

module.exports = {
  IP,
  PORT,
  message
};